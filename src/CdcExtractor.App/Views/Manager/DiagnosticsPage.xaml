<UserControl x:Class="CdcExtractor.App.Views.Manager.DiagnosticsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CdcExtractor.App.ViewModels"
             xmlns:controls="clr-namespace:CdcExtractor.App.Controls"
             d:DataContext="{d:DesignInstance vm:DiagnosticsViewModel}"
             mc:Ignorable="d">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Diagnostics"
                   Style="{StaticResource H1}"
                   Margin="0,0,0,8" />

        <!-- Description -->
        <TextBlock Grid.Row="1"
                   Text="Run diagnostic checks to verify SQL Server connectivity, permissions, downstream configuration, and IPC health."
                   TextWrapping="Wrap"
                   Style="{StaticResource Caption}"
                   Margin="0,0,0,16" />

        <!-- Run button and error -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Run Diagnostics"
                    Command="{Binding RunDiagnosticsCommand}"
                    Style="{StaticResource PrimaryButton}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"
                    Margin="0,0,12,0" />

            <TextBlock Text="Running..."
                       VerticalAlignment="Center"
                       Foreground="{StaticResource PrimaryBrush}"
                       FontSize="13"
                       Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisConverter}}" />

            <TextBlock Text="{Binding ErrorMessage}"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource ErrorBrush}"
                       FontSize="13"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />
        </StackPanel>

        <!-- Diagnostic results grouped by category -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding GroupedChecks}">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"
                                           Style="{StaticResource H2}"
                                           Margin="0,16,0,8" />
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Status chip + Name -->
                                <StackPanel Grid.Column="0" Grid.Row="0"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            Margin="0,0,16,0">
                                    <controls:StatusChip Status="{Binding Status}"
                                                         Margin="0,0,10,0" />
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               VerticalAlignment="Center" />
                                </StackPanel>

                                <!-- Detail -->
                                <TextBlock Grid.Column="1" Grid.Row="0"
                                           Text="{Binding Detail}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center" />

                                <!-- Remediation hint (only shown when present) -->
                                <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                                        Background="#FEF9C3"
                                        CornerRadius="4"
                                        Padding="10,6"
                                        Margin="0,8,0,0"
                                        Visibility="{Binding Remediation, Converter={StaticResource NullToCollapsedConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Remediation:"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="#CA8A04"
                                                   Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Remediation}"
                                                   FontSize="12"
                                                   Foreground="#92400E"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

        <!-- Inverse bool converter -->
        <local:InverseBoolConverter x:Key="InverseBoolConverter"
                                    xmlns:local="clr-namespace:CdcExtractor.App.Views.Manager" />

        <!-- Null-to-collapsed converter -->
        <local:NullToCollapsedConverter x:Key="NullToCollapsedConverter"
                                        xmlns:local="clr-namespace:CdcExtractor.App.Views.Manager" />
    </UserControl.Resources>

</UserControl>
