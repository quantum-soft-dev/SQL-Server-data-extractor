<UserControl x:Class="CdcExtractor.App.Views.Manager.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CdcExtractor.App.ViewModels"
             xmlns:controls="clr-namespace:CdcExtractor.App.Controls"
             d:DataContext="{d:DesignInstance vm:LogsViewModel}"
             mc:Ignorable="d">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Logs"
                   Style="{StaticResource H1}"
                   Margin="0,0,0,8" />

        <!-- Description -->
        <TextBlock Grid.Row="1"
                   Text="View and stream log entries from the extractor service."
                   TextWrapping="Wrap"
                   Style="{StaticResource Caption}"
                   Margin="0,0,0,16" />

        <!-- Error message -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource ErrorBrush}"
                   FontSize="13"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <!-- Log Console -->
        <controls:LogConsole Grid.Row="3"
                             ItemsSource="{Binding LogEntries}"
                             MinLevel="{Binding MinLevel, Mode=TwoWay}"
                             SearchText="{Binding SearchText, Mode=TwoWay}"
                             IsAutoScrollEnabled="{Binding IsAutoScroll, Mode=TwoWay}"
                             IsPaused="{Binding IsPaused, Mode=TwoWay}" />
    </Grid>

    <UserControl.Resources>
        <!-- Null-to-collapsed converter -->
        <local:NullToCollapsedConverter x:Key="NullToCollapsedConverter"
                                        xmlns:local="clr-namespace:CdcExtractor.App.Views.Manager" />
    </UserControl.Resources>

</UserControl>
